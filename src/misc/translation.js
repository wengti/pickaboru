
export function translation(inpt) {

    const word = inpt.toLowerCase()
    if (word === 'power') return 'パワー'
    else if (word === 'control') return 'コントロール'
    else if (word === 'balanced') return 'バランス'
}